[project]
name = "dms-retry"
version = "0.1.0"
description = "Geteilte Backoff-/Jitter-Mathematik (Full Jitter, AWS-Standardformel) für Retry-Poll-Loops (Post-Roadmap Phase 20 Session 1)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dms_retry"]
