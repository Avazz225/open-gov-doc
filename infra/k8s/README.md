# infra/k8s/

Platzhalter. Kubernetes-Manifeste werden erst relevant, sobald der Plugin Orchestration Service (Phase 10, Konzept 3.8) einen Plattform-Scheduler voraussetzt bzw. nutzt. Bis dahin läuft die lokale Entwicklung über `../docker-compose.yml`.
