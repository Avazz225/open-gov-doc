# Fortschritt

**Zuletzt abgeschlossen:** P3-S4 — Storage-Redundanz + Bereichssperren (Phase 3 abgeschlossen)
**Nächste Session:** P4-S1 — API-Gateway/BFF

Details je Session: siehe [`IMPLEMENTATION_PLAN.md`](IMPLEMENTATION_PLAN.md).

## Session-Tabelle

| Session | Status | Deliverable |
|---|---|---|
| P0-S1 | ✅ fertig | Repo-Skeleton, Git-Init, Docker-Compose-Grundgerüst, graphify-Setup |
| P0-S2 | ✅ fertig | Shared Libs, Service-Template, CI-Skeleton |
| P1-S1 | ✅ fertig | Registry Service |
| P1-S2 | ✅ fertig | Event-Bus produktiv + Audit Service Grundgerüst |
| P2-S1 | ✅ fertig | Keycloak + Auth Service |
| P2-S2 | ✅ fertig | Permission Service (RBAC) |
| P3-S1 | ✅ fertig | Storage Service + Backend-Plugin-Interface |
| P3-S2 | ✅ fertig | Document Service (CRUD, Versionierung, Locking) |
| P3-S3 | ✅ fertig | Folder Service + Object-Type Service + Constraint Engine |
| P3-S4 | ✅ fertig | Storage-Redundanz + Bereichssperren |
| P4-S1 | offen | API-Gateway/BFF |
| P4-S2 | offen | User-UI Grundgerüst |
| P4-S3 | offen | Admin-UI Grundgerüst + Doku-Pass → **MVP-Meilenstein** |
| P5–P14 | offen | siehe IMPLEMENTATION_PLAN.md (30 weitere Sessions) |

## Offene Entscheidungen

- **Suche-Backend** (fällig spätestens P5-S4): Postgres Full-Text-Search vs. dedizierter Suchindex — noch nicht entschieden.
- **SpiffWorkflow-Lizenz** (fällig vor P6-S1): LGPLv3 vs. Open-Source-Ziele des Gesamtsystems — rechtliche Prüfung steht aus (Konzept 13).
- **Paketmanager `uv`**: verifiziert in P0-S2 — `uv sync --all-packages` als Standardbefehl für den gesamten Workspace etabliert (Einzel-Package-Sync via `--package` entfernt sonst Dev-Dependencies aus der venv, bewusst vermieden).
- **pytest `--import-mode=importlib`**: ab P1-S2 fest in der Root-`pyproject.toml` gesetzt, da gleichnamige Testmodule (`test_repository.py`) über mehrere Services hinweg sonst kollidieren. Konsequenz: Tests dürfen keine Helfer per `from conftest import x` importieren (funktioniert mit importlib-Mode nicht) — stattdessen als Fixtures in `conftest.py` definieren (siehe `dms-auth-client/tests/conftest.py`).
- **AD-Gruppe → interne Rolle Mapping** (Konzept 4.4): weiterhin nicht implementiert. Permission Service (P2-S2) löst keine Gruppenmitgliedschaften auf — `principal_id` einer Zuweisung muss exakt dem abgefragten Principal entsprechen. Hängt am noch fehlenden AD-Sync im Auth Service.
- **Issuer-Hostname-Konsistenz Keycloak** (siehe `docs/services/auth-service.md`): aktuell unkritisch, da nur interner Password-Grant genutzt wird; bei Einführung eines browserbasierten Redirect-Flows (`standardFlowEnabled`) muss die Hostname-Konfiguration überdacht werden.
- **Struktur-Event-Vertrag Permission Service ↔ Folder Service** (siehe `docs/services/permission-service.md`): provisorisch (`folder.resource.created/.moved/.deleted`) — muss bei P3-S3 (Folder Service) bestätigt oder angepasst werden.
- **Granulare Cache-Invalidierung Permission Service**: aktuell wird bei jeder Rechte-/Strukturänderung der gesamte `effective_permission_cache` geleert statt nur der betroffene Teilbaum — bewusste Vereinfachung, spätere Optimierung möglich ohne API-Änderung.
- **Muster-Lehre `session.delete()` ohne `session.flush()`**: In P3-S1 gefunden (storage-service) — ein nachfolgendes `session.get()` auf denselben Primärschlüssel liefert das Objekt trotz `session.delete()` noch, wenn kein Flush dazwischenliegt (Identity-Map-Fast-Path). Registry Service macht es bereits richtig; bei künftigen Repository-Funktionen mit Delete+Re-Read im selben Test prüfen.
- **Force-Unlock ohne "überwachten" Lock-Zustand (Document Service, P3-S2)**: Konzept 4.2 beschreibt einen dritten Lock-Zustand ("aufgehoben, aber überwacht"); implementiert wurde stattdessen eine immer aktive optimistische Versionsprüfung beim Check-in, die dieselbe Garantie allgemeiner abdeckt (ADR 0002). Falls eine spätere Notification-Service-Integration (7) eine explizite "wer wurde benachrichtigt"-Verknüpfung zwischen Force-Unlock-Event und der ggf. entstehenden Konfliktkopie braucht, müssen beide `document.>`-Events (`lock.force_released` und `version.created`) dafür clientseitig über `subject=document_id` korreliert werden — aktuell nicht direkt verknüpft.
- **Vier-Augen-Prinzip für Force-Unlock (4.3)**: Der Force-Unlock-Endpunkt des Document Service ist bis P6-S4 (generischer Approval-Mechanismus) ungated und muss bis dahin auf API-Gateway-Ebene (P4-S1) restriktiv autorisiert werden.
- **Audit-Service-Abdeckung**: konsumiert weiterhin nur `registry.>` und `document.>`. Weder Auth-/Permission-/Storage- noch die neuen Folder-/Object-Type-Service-Events (`folder.>`) sind Teil des Audit-Trails — bei Bedarf einfach zur `subjects`-Liste ergänzen.
- **Kein Alembic, Ad-hoc-Schema-Migration (P3-S3)**: Document Service musste seiner bereits bestehenden `document`-Tabelle die Spalte `attributes` hinzufügen. Da `create_all` keine bestehenden Tabellen ändert, übernimmt ein `ALTER TABLE ... ADD COLUMN IF NOT EXISTS ... DEFAULT ... NOT NULL` im Lifespan-Startup diese eine additive Änderung idempotent. Funktioniert nur für neue, defaultbehaftete Spalten — Umbenennungen/Typänderungen/Spalten-Entfernungen brauchen echtes Migrationswerkzeug (Alembic). Nächste Session, die eine breaking Schemaänderung braucht, sollte Alembic einführen statt diesen Trick zu wiederholen.
- **Constraint Engine als Lib, nicht als Service** (ADR 0003): `dms-constraint-engine` ist eine reine Funktion, eingebettet ausschließlich im Object-Type Service; Document/Folder Service rufen dessen `/validate`-HTTP-Endpunkt auf. "Verweise auf andere Objekte" (`type: reference`) wird nur formal geprüft (nicht-leerer String), keine Existenzprüfung gegen den referenzierten Service — aufgeschoben, da eine generische Referenztyp→Service-Auflösung fehlt.
- **Kein Update-Endpunkt für Dokument-Attribute**: `attributes`/`title` eines Dokuments werden nur bei der Erstellung gesetzt und validiert, es gibt noch keine Möglichkeit, sie nachträglich zu ändern (und erneut zu validieren).
- **Storage-Redundanz auf zwei Ziele begrenzt** (ADR 0004): `local`+`s3` statt einer generischen Ziel-Menge — echte Mehrfach-Instanzen desselben Backend-Typs (z. B. zwei S3-Provider) brauchen eine neue Settings-Struktur (Liste statt fester Felder), die `replication.py`-Kernlogik ist dafür aber schon generisch genug.
- **Keine automatische periodische Ausführung** von Fixity-Checks (`/object-verify/.../all`) oder der Replikations-Retry-Queue (`/replication/process-pending`) — beide sind bewusst On-Demand-Endpunkte für einen künftigen externen Scheduler (frühestens Plugin Orchestration Service, Phase 10).
- **Bereichssperren ohne Zugriffskontrolle auf die eigenen Verwaltungsendpunkte**: `POST/DELETE /scope-locks` sind wie der Document-Service-Force-Unlock (P3-S2) ungated — reale Autorisierung ("nur bestimmte Admin-Rollen dürfen sperren") und ein optionales Vier-Augen-Prinzip (4.3) folgen auf API-Gateway-Ebene (P4-S1).
- **Rebalancing** beim Hinzufügen/Entfernen eines Storage-Backends aus einem laufenden Ziel-Set ist weiterhin offen (konzeptionell mit dem Migrationsprozess aus 7.2 verwandt) — eigene, spätere Session.

## Änderungslog

- **P0-S1**: Git-Repo in `dms/` initialisiert (Branch `main`). Monorepo-Skeleton angelegt (`services/`, `libs/`, `infra/`, `tools/cli/`, `docs/adr/`, `docs/services/`). `README.md`, `CONTRIBUTING.md`, `CLAUDE.md`, `.gitignore` geschrieben. `infra/docker-compose.yml` mit Postgres 16, NATS 2.10 (JetStream), Keycloak 25.0, MinIO erstellt und **verifiziert** (`docker compose up` → alle vier Container healthy, Keycloak erfolgreich gegen eigenes Postgres-Schema migriert), danach wieder heruntergefahren. `graphify` über das Konzeptdokument laufen lassen, `graphify claude install` + `graphify hook install` ausgeführt. Nächster Schritt: P0-S2 (Shared Libs + Service-Template + CI).
- **P0-S2**: uv-Workspace-Root (`pyproject.toml`, `.python-version`, Ruff-Config inkl. `ignore = ["B008"]` für FastAPIs `Depends(...)`-Idiom) angelegt. Vier Shared Libs gebaut, jede mit README + **echten** Tests (16 Tests insgesamt, alle grün):
  - `dms-common`: `BaseServiceSettings`, JSON-Logging, OTel-Tracing-Basis.
  - `dms-db-base`: Async-Engine, `make_declarative_base(schema)`, Session-Scope — Integrationstest gegen echtes Postgres (Docker).
  - `dms-eventbus-client`: `EventBusClient`-Interface + `NatsEventBusClient` — Integrationstest gegen echtes NATS JetStream (Docker), inkl. Publish/Subscribe-Roundtrip.
  - `dms-auth-client`: `TokenValidator` (JWKS/RS256) + FastAPI-Dependency — Unit-Tests mit selbst signiertem Testschlüssel (kein echter Keycloak nötig).
  - Docker-Testinfrastruktur nach jedem Lib-Test wieder heruntergefahren.
  `docs/service-template.md` geschrieben (Layout, `pyproject.toml`, `main.py`, Dockerfile) — **Konvention festgelegt**: Docker-Images kopieren `libs/` aus dem Monorepo und installieren lokal via `uv sync --frozen --package <name>` (kein interner Package-Index, volle Reproduzierbarkeit über `uv.lock`), Build-Kontext ist die Repo-Wurzel. `.github/workflows/ci.yml` (Postgres als Service-Container, NATS separat gestartet wegen JetStream-Flag, `ruff check .` + `pytest` über den ganzen Workspace) — lokal durchgespielt, nicht auf echtem GitHub Actions verifiziert. `graphify dms/ --update`: 167 Knoten/209 Kanten/31 Communities (Konzeptdokument-Knoten wie geplant gepruned, da `Business__DMS-Konzept.md`/`CLAUDE.md` jetzt gitignored sind). Ein Self-Loop-Edge im Graph als kleine Health-Warnung notiert, unkritisch. Nächster Schritt: P1-S1 (Registry Service).
- **P1-S1**: `services/registry-service` gebaut (erster echter Service, nach `docs/service-template.md`-Muster): FastAPI-App, eigenes Postgres-Schema `registry` (Tabelle `service_instance`), Endpunkte `POST /instances` (Register/Upsert), `POST /instances/{id}/heartbeat`, `DELETE /instances/{id}`, `GET /instances/{service_type}` (aktive Routingtabelle), `GET /instances`, `/healthz`. **Design-Entscheidung**: Ausfallerkennung wird beim Lesen berechnet (`last_heartbeat_at` vs. `heartbeat_timeout_seconds`, Default 15s) statt über einen mutierenden Hintergrund-Sweep — vermeidet Race Conditions, einfacher zu testen, funktional äquivalent zu "fällt aus der Routingtabelle". Lizenzvermittlung (3.2b) bewusst noch nicht enthalten, folgt mit dem License Service (Phase 9). 14 Tests (Repository + API, gegen echtes Postgres) grün. Dockerfile nach Service-Template gebaut und **verifiziert**: Image gebaut, im Compose-Stack mit Postgres hochgefahren, End-to-End-Smoke-Test per `curl` (Register → Routingtabelle-Abfrage) erfolgreich, danach heruntergefahren. `docs/services/registry-service.md` angelegt. Gesamter Workspace (30 Tests über 4 Libs + Registry Service) läuft grün, Lint sauber. `graphify --update` bewusst noch nicht gelaufen (laut Plan erst am Ende der Phase, also nach P1-S2, verpflichtend). Nächster Schritt: P1-S2 (Event-Bus produktiv + Audit Service).
- **P1-S2**: `dms-eventbus-client` erweitert: `Event`-Envelope (gemeinsames Wire-Format, `to_bytes()`/`from_bytes()`) sowie **ADR 0001** — `NatsEventBusClient` unterscheidet jetzt Producer (`ensure_stream=True`, Default) von reinen Konsumenten (`ensure_stream=False`, kein eigener Stream nötig, JetStream löst den Stream über das Subject auf), außerdem `deliver_new`-Flag für Subscriptions, die keinen Nachrichtenverlauf wollen (nötig für saubere Testisolation). Registry Service publiziert jetzt `registry.instance.registered`/`registry.instance.deregistered` nach Commit (kein Event pro Heartbeat). Neuer **Audit Service** (`services/audit-service`): hash-verkettetes Ereignisprotokoll (`sha256(prev_hash + kanonisches_JSON(...))`, Genesis-Hash bei erstem Eintrag), idempotent nach `event_id` (At-least-once-Redelivery durch JetStream), konsumiert konfigurierbare Subjects (Default `["registry.>"]`) über einen durable Consumer ohne eigenen Stream, API `GET /events`, `GET /events/verify` (Kettenprüfung, erkennt Manipulation testweise nachgewiesen). 33 neue Tests (Hash-Chain-Unit-Tests, Repository inkl. Tamper-Detection, Consumer-Integration gegen echtes NATS+Postgres, Registry-Event-Publishing-Tests) — gesamter Workspace jetzt **48 Tests, alle grün**. **Zwischenfall & Fix**: `pytest`-Kollision zwischen gleichnamigen Testmodulen (`test_repository.py`) über mehrere Services hinweg → `--import-mode=importlib` in der Root-`pyproject.toml` gesetzt, dafür mussten `dms-auth-client`s Test-Helfer von Modul-Import auf Fixtures umgestellt werden. **Zwischenfall & Fix**: End-to-End-Smoke-Test im Compose-Stack zeigte zunächst keine Events, da `docker compose up -d` das Registry-Service-Image aus P1-S1 nicht neu gebaut hatte (alter Code ohne Event-Publishing lief noch) — Lehre in `CONTRIBUTING.md` festgehalten: nach Code-Änderungen vor dem Smoke-Test immer explizit `docker compose build <service>`. Nach Fix: End-to-End im Compose-Stack verifiziert (Registrierung → Event publiziert → Audit Service konsumiert und verkettet → `/events/verify` → `ok: true`). `docs/services/audit-service.md`, READMEs aktualisiert. `graphify --update` (Phasenabschluss P1, verpflichtend): 327 Knoten/454 Kanten/47 Communities. → **Phase 1 (Control-Flow-Fundament) abgeschlossen.** Nächster Schritt: P2-S1 (Keycloak + Auth Service).
- **P2-S1**: Neuer **Auth Service** (`services/auth-service`) als schlanker OIDC-Broker vor Keycloak (4.4) — kein eigenes Postgres-Schema, keine eigene Nutzertabelle (Keycloak deckt lokale + AD-föderierte Konten bereits nativ ab). `ensure_realm_and_client()` legt bei jedem Start idempotent Realm `dms` und Confidential Client `dms-api` an (`python-keycloak`/`KeycloakAdmin`), inkl. eines Audience-Mappers (ohne den trägt der Access-Token nur `aud: "account"`, nicht den eigenen Client-Namen — Keycloak-Default). Endpunkte `POST /login` (Password-Grant), `POST /refresh`, `GET /me` (Token-Validierung über `dms-auth-client`s `TokenValidator` gegen Keycloaks JWKS, zustandslos), `/healthz`. **Zwei über interaktives Sondieren gefundene Keycloak-Fallen** (vor dem Schreiben der Tests direkt gegen echtes Keycloak verifiziert, nicht erraten): (1) neue Nutzer ohne `firstName`/`lastName` lösen beim Login "Account is not fully set up" aus, da Keycloak 25s Default-User-Profile diese Felder verlangt (`VERIFY_PROFILE`); (2) Access-Tokens tragen ohne expliziten Audience-Mapper nur `aud: "account"` statt des eigenen Client-Namens. Beide Fallen in `docs/services/auth-service.md` und `README.md` dokumentiert. Keycloak bekam einen echten Healthcheck (kein curl/wget im Image → roher HTTP-Check über `bash /dev/tcp` gegen `/realms/master`), nötig für `depends_on: condition: service_healthy` des Auth Service. 9 neue Tests (Bootstrap-Idempotenz, Login/Refresh/Me-Flows gegen echtes Keycloak) — gesamter Workspace jetzt **57 Tests, alle grün**. End-to-End im Compose-Stack verifiziert (Login → Access-Token → `/me` liefert korrekte Identität inkl. Keycloak-Rollen). AD-Gruppe→interne-Rolle-Mapping bewusst nicht implementiert, verschoben auf Permission Service (P2-S2). `graphify --update` bewusst noch nicht gelaufen (Phase 2 noch nicht abgeschlossen). Nächster Schritt: P2-S2 (Permission Service).
- **P2-S2**: Neuer **Permission Service** (`services/permission-service`, Konzept 4.1): RBAC mit `Role` (Name + `permissions`-Capability-Liste), `RoleAssignment` (Principal × Rolle × Ressource), `ResourceNode` (generische Hierarchie, `parent_id` + `inherit`-Flag), `EffectivePermissionCache` (materialisiert). **Vererbungsalgorithmus**: Aufstieg von der angefragten Ressource über `parent_id` bis zur Wurzel (`root`, beim Start automatisch angelegt), sammelt Zuweisungen je Knoten; `inherit=false` beendet den Aufstieg *nach* Auswertung der eigenen Zuweisungen des Knotens (SharePoint/Alfresco-Standardverhalten). Cache-Invalidierung bewusst grobkörnig (kompletter Cache statt nur betroffener Teilbaum) — einfacher, korrekt, dokumentierte Vereinfachung. **Architektur-Herausforderung**: Der Folder Service (P3-S3), der die Ressourcen-Hierarchie eigentlich besitzen wird, existiert noch nicht — Permission Service definiert daher einen **provisorischen Struktur-Event-Vertrag** (`folder.resource.created/.moved/.deleted`, dokumentiert in `docs/services/permission-service.md`) und konsumiert ihn bereits jetzt über `structure_consumer.py`, verifiziert per synthetisch publizierten Events (Rolle einer nicht-existenten "Folder Service" simulierend). **Zwischenfall & Fix (lib-Erweiterung)**: Der Service startete beim ersten Anlauf mit `NotFoundError`, weil `ensure_stream=False`-Konsumenten (ADR 0001) beim Abonnieren scheitern, wenn noch nie ein Producer den Stream angelegt hat (kein Folder Service ist je gelaufen) — `dms-eventbus-client` bekam dafür eine eigene, backend-unabhängige `SubjectNotFoundError` (übersetzt aus NATS' `NotFoundError`), und `start_consuming()` überspringt betroffene Subjects statt den Service-Start zu blockieren (kein Retry-Loop, Neustart nötig, sobald der Stream später auftaucht — dokumentierte Grenze). 24 neue Tests (Repository inkl. Vererbung/Override/Cache-Invalidierung, Structure-Consumer-Integration gegen echtes NATS, API) plus 1 neuer Test in `dms-eventbus-client` für die neue Exception — gesamter Workspace jetzt **76 Tests, alle grün**. End-to-End im Compose-Stack verifiziert: synthetisches `folder.resource.created`-Event → Ressource angelegt → Rolle an Wurzel zugewiesen → an der Unterressource korrekt geerbt → `/check` liefert `allowed: true`. `graphify --update` (Phasenabschluss P2, verpflichtend): 480 Knoten/749 Kanten/50 Communities. → **Phase 2 (Auth & Permissions) abgeschlossen.** Nächster Schritt: P3-S1 (Storage Service + Backend-Plugin-Interface).
- **P3-S1**: Neuer **Storage Service** (`services/storage-service`, Konzept 3.6): `StorageBackend`-Interface (`write`/`read`/`delete`/`exists`/`checksum`) mit zwei Implementierungen. **`LocalFilesystemBackend`** deckt bewusst sowohl "lokales Dateisystem" als auch **NFS** ab — in Kubernetes ist der konfigurierte Pfad einfach der Mountpunkt eines PVC, das Backend sieht nur einen Ordner unabhängig davon, was darunterliegt (Nutzerhinweis in dieser Session aufgegriffen, kein separates NFS-Backend gebaut). Schreibt atomar (temp-Datei + `os.replace`) statt mit `fcntl`-Locking, dessen Semantik über NFS-Implementierungen hinweg inkonsistent ist. **`S3Backend`** (`aioboto3`) gegen MinIO getestet, funktioniert identisch gegen jeden S3-kompatiblen Provider. Metadaten (`object_key`, `backend`, `checksum_sha256`, `size_bytes`, `content_type`) im eigenen Schema `storage`; Inhalte selbst nie in der DB. API: `PUT`/`GET`/`DELETE /objects/{key:path}`, `GET /object-metadata/{key:path}`, `GET /object-verify/{key:path}` (Fixity-Check-Grundlage: Prüfsumme aus dem Backend neu lesen, mit DB-Referenzwert vergleichen). Storage-Redundanz (mehrere Ziele, Quorum-Schreiben) bewusst nicht Teil dieser Session, folgt in P3-S4. **Zwischenfall & Fix**: `repository.delete_metadata` fehlte ein `session.flush()` nach `session.delete()` — ohne Flush lieferte ein nachfolgendes `session.get()` auf denselben Primärschlüssel das Objekt trotzdem noch (Identity-Map-Fast-Path umgeht die Query), ein Test deckte das auf. 46 neue Tests (Local-Backend gegen echtes Dateisystem, S3-Backend gegen echtes MinIO, Repository gegen echtes Postgres, API End-to-End) — gesamter Workspace jetzt **106 Tests, alle grün**. **End-to-End im Compose-Stack für BEIDE Backends verifiziert** (explizit vom Nutzer gefordert): Upload/Download/Verify über die echte HTTP-API zunächst mit `local`-Backend (Datei im gemounteten Volume nachgewiesen), danach derselbe Container mit `DMS_BACKEND=s3` neu gestartet und derselbe Ablauf gegen MinIO wiederholt (Objekt im MinIO-Datenverzeichnis nachgewiesen). `docs/services/storage-service.md` angelegt. `graphify --update` bewusst noch nicht gelaufen (Phase 3 noch nicht abgeschlossen). Nächster Schritt: P3-S2 (Document Service).
- **P3-S2**: Neuer **Document Service** (`services/document-service`, Konzept 2.1/2.1a/4.2): `Document` (Metadaten + `current_version_number`-Zeiger), `DocumentVersion` (jede Version bleibt für immer abrufbar, 2.1a — auch Konfliktkopien), `DocumentLock` (an Nutzer+Session gebunden, Ablauf wird beim Zugriff bewertet statt per Hintergrund-Sweep, analog Registry Service). Hält selbst keine Dateiinhalte — spricht Storage Service (P3-S1) ausschließlich über dessen HTTP-API an, Objektschlüssel sind inhaltsadressiert (`documents/{id}/{sha256}`), was die Henne-Ei-Reihenfolge "Schlüssel braucht Versionsnummer, Versionsnummer braucht abgeschlossenen DB-Write" vermeidet und identische Inhalte dedupliziert. **Architektur-Entscheidung (ADR 0002)**: Das Konzept beschreibt Force-Unlock über einen dritten Lock-Zustand ("aufgehoben, aber überwacht"); stattdessen löscht Force-Unlock die Sperre vollständig, und derselbe Schutz vor stillem Datenverlust entsteht durch eine immer aktive **optimistische Konflikterkennung** beim Check-in (`expected_base_version_number`): weicht die angegebene Ausgangsversion von der aktuellen Hauptversion ab, entsteht eine eigenständige Konfliktkopie (`<name>_conflict_<user>_<zeitstempel>`) statt eines stillen Überschreibens — deckt denselben und allgemeinere Fälle ab (z. B. Wettlauf ganz ohne Sperre) mit einem einzigen Mechanismus statt zwei. API: Upload/Get/Delete, Versionsliste/-inhalt, Lock/Unlock/Force-Release, publiziert `document.created`/`.version.created`/`.lock.force_released`/`.deleted`. **4.2 verlangt explizit vollständige Auditierung von Force-Unlock** — Audit Service konsumiert daher ab dieser Session zusätzlich `document.>` (vorher nur `registry.>`). **Zwischenfall & Fix (Audit Service)**: Das zweite konsumierte Subject deckte einen echten Nebenläufigkeits-Bug auf — NATS-Callbacks für `registry.>` und `document.>` können gleichzeitig laufen, `append_event` las den Kettenkopf (`prev_hash`) aber ohne Serialisierung, wodurch zwei parallele Appends dieselbe Vorgänger-Hash lesen und die Kette korrumpieren konnten (aufgedeckt durch einen zuvor stabilen Test, der plötzlich fehlschlug, weil beim Testlauf bereits aufgelaufene `document.*`-Nachrichten parallel zum injizierten Testereignis verarbeitet wurden). Fix: `audit_service/consumer.py` serialisiert alle `append_event`-Aufrufe über einen In-Prozess-`asyncio.Lock` je Handler-Instanz (Audit Service ist als Single-Writer für seine eigene Kette konzipiert, keine horizontale Skalierung vorgesehen). 25 neue Tests im Document Service (Repository inkl. Lock-Konflikt/Force-Unlock/Konfliktkopie-Szenarien gegen echtes Postgres, API End-to-End gegen echten Storage Service über HTTP, Event-Publishing gegen echtes NATS) — gesamter Workspace jetzt **131 Tests, alle grün**. End-to-End im Compose-Stack verifiziert: Upload → Lock → Force-Unlock (Admin) → Bobs regulärer Check-in wird neue Hauptversion → Alices Check-in auf Basis der überholten Version landet sichtbar als Konfliktkopie, Hauptversion bleibt Bobs Stand → Audit Service zeigt beide Ereignisse (`lock.force_released` + `version.created` mit `is_conflict: true`) im `/events`-Trail, `/events/verify` bestätigt `ok: true` über die gemischte, nebenläufig eingetroffene Ereignishistorie. `docs/services/document-service.md`, `docs/adr/0002-document-locking-optimistic-conflict-detection.md`, READMEs (Document Service + Audit Service) angelegt/aktualisiert. `graphify --update` bewusst noch nicht gelaufen (Phase 3 noch nicht abgeschlossen). Nächster Schritt: P3-S3 (Folder Service + Object-Type Service + Constraint Engine).
- **P3-S4**: Letzte Session der Phase 3 (Konzept 3.6/4.7). **Storage Service erweitert** um Redundanz: `object_copy`-Tabelle (eine Zeile je Objekt × konfiguriertem Ziel, Status `pending`/`ok`/`failed`/`failed_permanent`), neue `replication.py`-Orchestrierung, backend-agnostisch (`dict[str, StorageBackend]` + `list[str]` Zielpriorität), unabhängig von der FastAPI-Singleton-Konfiguration testbar. Zwei Schreibstrategien: `quorum` (synchron auf alle Ziele, Erfolg erst ab `quorum_count` Bestätigungen, bei Nichterreichen werden bereits erfolgreiche Teilkopien best-effort zurückgerollt, fehlgeschlagene Ziele bleiben als Diagnose-Eintrag) und `primary_async` (Werkseinstellung für den Arbeitsbetrieb: nur Primärziel synchron, Sekundärziel `pending`, nachgezogen über den neuen Endpunkt `POST /replication/process-pending` — Retry-Queue statt In-Prozess-Hintergrundtask). Lese-Fallback (`GET /objects/{key}` probiert Ziele in Priorität, überspringt nicht-`ok`-Kopien) und Fixity-Check über alle Kopien (`GET /object-verify/{key}/all`, aktualisiert `object_copy` bei Prüfsummen-Abweichung) ergänzen die im Konzept geforderte Basis. **Architektur-Entscheidung (ADR 0004)**: Redundanz bewusst auf genau zwei Ziele (`local`+`s3`, die einzigen existierenden Backend-Typen) statt einer generischen Ziel-Menge begrenzt, und die Retry-Queue bewusst über einen expliziten, deterministisch testbaren Endpunkt statt eines Timing-abhängigen In-Prozess-Hintergrundtasks — beides ausführlich begründet inkl. Migrationspfad zu echter Mehrfach-Ziel-Konfiguration. **Permission Service erweitert** um Bereichssperren (4.7): neue `ScopeLock`-Tabelle (nie hart gelöscht, Aufhebung wird dokumentiert), `repository.get_active_scope_locks_for_resource` läuft dieselbe Vorfahrenkette wie die RBAC-Auswertung ab, bricht aber **nicht** bei `inherit=false` ab (Bereichssperren gelten immer für den ganzen Unterbaum, unabhängig von der RBAC-Vererbungssteuerung). `GET /check` bekam einen `access_type`-Parameter (`read`|`write`, Default `write`) und blockiert bei aktiver Sperre unabhängig von den eigentlich zugewiesenen Rechten (`blocked_by_scope_lock`, `scope_lock_reason`, `scope_lock_expires_at` in der Antwort) — außer der Principal hat die neu eingeführte Capability `scope_lock.bypass`. Setzen/Aufheben publiziert `permission.scope_lock.created`/`.released` über einen neuen, vom bestehenden Struktur-Konsumenten getrennten Producer-Client (eigener Stream `permission`) — der **Audit Service konsumiert seit dieser Session zusätzlich `permission.>`** (4.7 verlangt explizit vollständige Auditierung). Wie beim Document-Service-Force-Unlock (P3-S2) sind die Verwaltungsendpunkte selbst ungated, reale Autorisierung folgt auf API-Gateway-Ebene (P4-S1). 51 neue Tests (Storage-Service-Repository/-Replication/-API inkl. echter Fehlerpfade über eine bewusst immer scheiternde, aber reale Backend-Interface-Implementierung statt Mocking; Permission-Service-Repository/-API/-Events gegen echtes Postgres+NATS) — gesamter Workspace jetzt **228 Tests, alle grün**. End-to-End im Compose-Stack verifiziert: Quorum-Schreiben über echtes lokales Dateisystem **und** echtes MinIO gleichzeitig (beide Kopien `ok`, Fixity-Check bestätigt beide Prüfsummen) → Umschalten auf `primary_async` → Sekundärkopie zunächst `pending`, Download funktioniert bereits über die Primärkopie, `POST /replication/process-pending` zieht die MinIO-Kopie nach (`succeeded: 1`) → Document Service läuft transparent gegen den redundanten Storage Service weiter, ohne Codeänderung nötig zu haben → Bereichssperre am Wurzelknoten blockiert Schreibzugriff mit Grund in der Antwort, Lesezugriff bleibt erlaubt (da `blocks_read=false`), nach Aufheben sofort wieder ursprünglicher Zustand → Audit Service (frisch gestartet, ohne `deliver_new`) holt die komplette `permission.>`-Historie lückenlos nach, `/events/verify` bestätigt `ok: true` über 55 gemischte Ereignisse aus Live-Test und vorherigen Pytest-Läufen. `docs/services/{storage,permission,audit}-service.md`, `docs/adr/0004-storage-redundancy-scope.md`, alle betroffenen READMEs, `infra/docker-compose.yml`/`.env.example` aktualisiert. → **Phase 3 (Storage & Dokumenten-Kern) abgeschlossen.** `graphify dms/ --update` (Phasenabschluss, verpflichtend) im Anschluss ausgeführt. Nächster Schritt: P4-S1 (API-Gateway/BFF) — erste Session der Phase 4, die mit dem MVP-Meilenstein (P4-S3) endet.
- **P3-S3**: Drei neue Bausteine (Konzept 2.1/2.2/4.5). **`libs/dms-constraint-engine`**: reine, zustandslose Validierungsfunktion (`schema, name, attributes -> errors`) — Pflichtfelder, bedingte Pflichtfelder (restriktiver `attr op literal`-Parser, kein `eval()`), Musterprüfung (Regex) für Werte und Namen (Platzhalter wie `{Rechnungsnummer}` werden durch den tatsächlichen Attributwert ersetzt), Wertebereiche (`min`/`max`) — die in 4.5 geforderten Mindest-Regeltypen. **Neuer Object-Type Service**: persistiert Objekttyp-Definitionen (2.2) und bietet `POST /object-types/{id}/validate` als einzigen Konsumenten der Lib an. **Architektur-Entscheidung (ADR 0003)**: Die "Constraint Engine" wurde bewusst nicht als eigener Microservice gebaut, sondern als Lib im Object-Type Service eingebettet — reine Funktion ohne eigenen Zustand/Skalierungsbedarf, ein Service nur für eine zustandslose Validierung hätte keinen architektonischen Vorteil geboten. **Neuer Folder Service**: Ordner-Hierarchie (2.1) mit CRUD, Verschieben (mit Selbst-Referenz-Schutz), Löschen nur wenn leer (409 sonst), optionaler Objekttyp-Validierung beim Anlegen (ruft Object-Type Service über HTTP auf). Publiziert exakt den Struktur-Event-Vertrag, den der Permission Service seit P2-S2 provisorisch erwartet hatte (`folder.resource.created/.moved/.deleted`) — **keine Anpassung nötig**, live end-to-end verifiziert: ein über die echte Folder-Service-API angelegter Ordner erscheint unmittelbar im `resource_node`-Baum des Permission Service mit korrektem `parent_id`. Permission-Service-Doku von "provisorisch" auf "bestätigt" aktualisiert. **Document Service erweitert**: neue Spalte `attributes` (JSON, Custom-Felder gemäß Objekttyp); `folder_id` wird jetzt beim Anlegen gegen den Folder Service auf Existenz geprüft (400 bei unbekannter ID), `object_type_id`+`attributes` gegen den Object-Type Service validiert (400 mit Fehlerliste bei Verstoß) — beide Prüfungen entfallen, wenn das jeweilige Feld nicht gesetzt ist. Objektschlüssel bleiben inhaltsadressiert, keine Änderung an der Storage-Anbindung. **Zwischenfall & Fix (Schema-Evolution ohne Alembic)**: Die neue `attributes`-Spalte auf der bereits existierenden `document`-Tabelle (aus P3-S2, Postgres-Volume blieb zwischen Sessions erhalten) wurde von `create_all` nicht angelegt (legt nur fehlende Tabellen an, keine fehlenden Spalten) — behoben mit einem idempotenten `ALTER TABLE ... ADD COLUMN IF NOT EXISTS ... DEFAULT ... NOT NULL` im Lifespan-Startup; als Grenze dieses Ad-hoc-Ansatzes in "Offene Entscheidungen" festgehalten (Alembic wird nötig, sobald eine Änderung komplexer als additiv+defaultbehaftet ist). 61 neue Tests (Constraint-Engine-Lib, Object-Type Service Repository+API, Folder Service Repository+API+Events+Objekttyp-Integration gegen echten Object-Type Service, Document Service Metadata-Integration gegen echte Folder-/Object-Type-Services) — gesamter Workspace jetzt **187 Tests, alle grün**. End-to-End im Compose-Stack verifiziert: Objekttyp mit bedingter Pflichtangabe angelegt → Dokument-Upload ohne Pflichtangabe korrekt mit 400 abgelehnt → mit vollständigen Attributen akzeptiert → Ordner erscheint live im Permission Service → Audit-Trail zeigt alle `document.>`-Ereignisse, Kette bleibt über 60 Einträge hinweg verifiziert (`ok: true`). `docs/services/{object-type,folder}-service.md`, `docs/adr/0003-constraint-engine-as-library.md`, alle betroffenen READMEs aktualisiert. `graphify --update` bewusst noch nicht gelaufen (Phase 3 noch nicht abgeschlossen, folgt nach P3-S4). Nächster Schritt: P3-S4 (Storage-Redundanz + Bereichssperren) — **letzte Session vor dem Phase-3-Abschluss und dem verpflichtenden `graphify --update`**.
